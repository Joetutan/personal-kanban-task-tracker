[project]
name = "personal-kanban-task-tracker"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dotenv>=0.9.9",
    "psycopg>=3.3.4",
    "typer>=0.26.8",
]

[project.scripts]
todo = "app.main:main"
